
language: node_js
node_js:
- "8.1.3"

branches:
  only:
  - master

before_script:
- npm install -g firebase-tools
- npm install -g preact-cli

script:
- echo "Husnain"

deploy:
- firebase deploy — project ci-testing-4d6f0 — token $FIREBASE_TOKEN.

notifications:
  email:
    on_failure: change
    on_success: change